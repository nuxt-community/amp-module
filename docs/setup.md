# Setup

Install with yarn:

```bash
yarn add @nuxtjs/amp
```

Install with npm:

```bash
npm install @nuxtjs/amp
```

Edit `nuxt.config.js`:

```js
modules: [
  '@nuxtjs/amp'
],

amp: {
  // Options
}
```
